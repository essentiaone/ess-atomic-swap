{
  "name": "ess-atomic-swap",
  "description": "Service to cross-chain transactions between two or more people",
  "scripts": {
  },
  "env": {
		"BTC_ADDR": {
      "required": true
    },
		"ETH_ADDR": {
      "required": true
    },
		"SCDB_ADDR": {
      "required": true
    },
		"PRIVATE_KEY": {
      "required": true
    }
  },
  "formation": {
    "web": {
      "quantity": 1
    }
  },
  "addons": [

  ],
  "buildpacks": [
    {
      "url": "heroku/go"
    }
  ]
}
